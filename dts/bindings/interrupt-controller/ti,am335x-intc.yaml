# SPDX-License-Identifier: Apache-2.0

description: TI AM335x Interrupt Controller (INTC)

compatible: "ti,am335x-intc"

include: interrupt-controller.yaml

properties:
  reg:
    type: array
    required: true

  "#interrupt-cells":
    type: int
    const: 1

interrupt-cells:
  - irq
